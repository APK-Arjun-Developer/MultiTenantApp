import Typography from '@mui/material/Typography';

export function ResetPasswordPage() {
  return (
    <Typography variant="body2" color="text.secondary" align="center">
      Reset password form — Phase 5
    </Typography>
  );
}
